@page
@model ErrorModel
@{
    ViewData["Title"] = "错误";
}

<h1 class="text-danger">错误。</h1>
<h2 class="text-danger">处理请求时发生错误。</h2>

@if (Model.ShowRequestId)
{
    <p>
        <strong>请求 ID：</strong> <code>@Model.RequestId</code>
    </p>
}

<h3>开发模式</h3>
<p>
    切换到 <strong>开发</strong> 环境将显示发生的详细错误信息。
</p>
<p>
    <strong>对于已部署的应用程序，不应启用开发环境。</strong>
    这可能导致向最终用户显示异常的敏感信息。
    对于本地调试，可通过将 <strong>ASPNETCORE_ENVIRONMENT</strong> 环境变量设置为 <strong>Development</strong> 并重启应用程序来启用 <strong>开发</strong> 环境。
</p>